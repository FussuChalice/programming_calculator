import 'package:flutter/material.dart';

class CalculatorBoardDivider extends StatelessWidget {
  const CalculatorBoardDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 4, color: Colors.black);
  }
}
